{
  "private": true,
  "scripts": {
    "lint": "turbo lint",
    "test": "turbo test",
    "tsc": "turbo tsc",
    "build": "turbo build",
    "dev": "turbo dev --no-cache --continue",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "description": "marching-squares is a JavaScript project designed for testing SVG and WebGL rendering of isolines",
  "keywords": [
    "isoline",
    "react",
    "marching squares",
    "react-component",
    "svg",
    "webgl",
    "data visualization"
  ],
  "devDependencies": {
    "eslint": "^8.45.0",
    "eslint-config-base": "workspace:*",
    "prettier": "^2.8.8",
    "turbo": "^1.11.0"
  },
  "packageManager": "pnpm@8.5.1"
}
